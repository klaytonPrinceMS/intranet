"""Filas module — multi-queue call manager with TV per queue, group and room.

EN: Multi-queue call manager with TV display (routes /filas, /tv, /tv/{id});
    own database db_mod_filas.db; no cross-query. See docs/modulos/filas.md.

Módulo Filas — gestor de chamadas multi-filas com TV por fila, grupo e sala
(rotas /filas, /tv, /tv/{id}); banco próprio db_mod_filas.db; sem cross-query.
Ver docs/modulos/filas.md.
"""

SKELETON = True
