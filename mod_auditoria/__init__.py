"""Audit module — exclusive LGPD trail database (one table per module) and read-only viewer.

Módulo Auditoria — banco exclusivo da trilha LGPD (uma tabela por módulo)
e visualizador somente-leitura em /auditoria."""
